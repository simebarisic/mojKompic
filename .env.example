# Za lokalni dev (SQLite) ne treba ništa postaviti — samo pokreni npm run dev.
# Za Docker/Postgres postavi DATABASE_URL (docker-compose.yml je već postavlja umjesto tebe):
# DATABASE_URL=postgres://kompic:change-me@db:5432/kompic

PORT=3001
